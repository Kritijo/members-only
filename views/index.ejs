<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="root.css" />
    <link rel="stylesheet" href="index.css" />
    <title>Members-Only</title>
  </head>
  <body>
    <% if(user){ %> <%- include("greetings") %> <% } else{ %> <%-
    include("initial") %> <% } %>
  </body>
</html>
